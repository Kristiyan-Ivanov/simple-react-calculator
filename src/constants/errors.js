const ErrorType = {
  MAX_DIGITS: 'Maximum number of digits reached.',
  DIV_BY_ZERO: 'Division by zero is not allowed.',
  MAX_DISPLAY: 'Maximum display limit reached.',
};

export default ErrorType;
